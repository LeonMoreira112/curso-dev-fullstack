// Functions

function escreverNoConsole (mensagem) {
    console.log(mensagem)
}

//Encapsular um código para ele não ser executado na mesma hora que a página abre, e que ele seja executado somente quando eu quiser, nesse caso, quando o usuário clicar no botão

// Outras funções:

console.log(parseInt('12')) //Transorma uma string em number
//console.log() / Executa um argumento
//document.querySelector() // Executa um elemento