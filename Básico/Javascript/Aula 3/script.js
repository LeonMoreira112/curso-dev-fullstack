// Var, let, const : Variáveis

//var nome= 'Thiago' // String (Frases, nomes, nomes compostos) Precisa de ' '

//var idade = 35    // number (número) Não precisa de ' ' para que seja entendido com um número

console.log(idade) // abre a caixinha var : Mostra o nome (Thiago) no console

// Var Está caindo em desuso : Usamos o let (O let declara uma variável e tem um funcionamento dentro de escopo)
// const : é uma constante Não pode ser alterado, serve para manter uma concistência no nosso sistema

const nomeCompleto = 'Thiago' //Camel case : As variáveis não podem ter nome composto, separar (Nome Completo). Também n pode começar com número

let